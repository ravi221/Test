import { SortWithComparatorPipe } from './sort-with-comparator.pipe';

describe('SortWithComparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new SortWithComparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
