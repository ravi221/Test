import { NotHiddenPipe } from './not-hidden.pipe';

describe('NotHiddenPipe', () => {
  it('create an instance', () => {
    const pipe = new NotHiddenPipe();
    expect(pipe).toBeTruthy();
  });
});
